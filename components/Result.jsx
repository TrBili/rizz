export default function Result(props){
    return(
        <div>
            Your score: {props.score}
        </div>
    )
}

// you have props which allows you to extend 
// parameters


// hooks:
// React features: state, component life cycle, etc

// useStates
// const [state name, state updateing function name]= useState(initial value)
